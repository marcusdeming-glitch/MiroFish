services:
  mirofish:
    build: .
    container_name: mirofish
    env_file:
      - .env
    ports:
      - "8080:3000"
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - ./data:/app/backend/uploads
